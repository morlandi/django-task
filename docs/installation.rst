============
Installation
============

At the command line::

    $ easy_install django-task

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-task
    $ pip install django-task
